<?php

use Illuminate\Foundation\Testing\RefreshDatabase;

uses(RefreshDatabase::class);

it('renders the {{ route }} index page', function (): void {
    $this->get(route('{{ route }}.index'))
        ->assertOk();
});
